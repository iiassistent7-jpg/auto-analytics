[phases.setup]
nixPkgs = ["...", "libuuid", "libglib", "nss", "nspr", "dbus", "atk", "cups", "libdrm", "expat", "libxcb", "libxkbcommon", "pango", "cairo", "alsa-lib", "at-spi2-atk", "mesa"]

[phases.install]
cmds = ["pip install -r requirements.txt", "playwright install --with-deps chromium"]
