[phases.setup]
nixPkgs = ["chromium"]

[phases.install]
cmds = ["pip install -r requirements.txt", "playwright install chromium"]

[start]
cmd = "python agent.py"
```

Также нужно добавить переменную в Railway Variables:
```
PLAYWRIGHT_BROWSERS_PATH=/root/.cache/ms-playwright
